import 'package:flutter/material.dart';

class SoldTab extends StatelessWidget {
  const SoldTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Sold'));
  }
}
