import 'package:flutter/material.dart';

class SellingTab extends StatelessWidget {
  const SellingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Selling'));
  }
}
